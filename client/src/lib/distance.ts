/**
 * Re-export distance utilities from shared module
 * This keeps existing client imports working
 */
export { calculateDistance, isWithinBumpingDistance, formatDistance } from "@shared/distance";
